import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';
import './index.css';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);

//first of all, create a folder for all components, and inside it create one for all the UI, and other to Users information

//inside this Users, create a AddUser.js component to get all the user's information

//inside UI folder, create a Card.js component to add some styles on the forms.

//create the Card.module.css to make the styles of the card.
